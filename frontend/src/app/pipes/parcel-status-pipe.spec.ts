import { ParcelStatusPipe } from './parcel-status-pipe';

describe('ParcelStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ParcelStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
