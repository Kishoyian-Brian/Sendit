<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>New Contact Form Submission</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #BA0C2F; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background-color: #f9f9f9; }
        .field { margin-bottom: 15px; }
        .label { font-weight: bold; color: #BA0C2F; }
        .value { margin-top: 5px; }
        .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>New Contact Form Submission</h1>
        </div>
        
        <div class="content">
            <p>A new contact form has been submitted on the SendIt website.</p>
            
            <div class="field">
                <div class="label">Name:</div>
                <div class="value"><%= name %></div>
            </div>
            
            <div class="field">
                <div class="label">Email:</div>
                <div class="value"><%= email %></div>
            </div>
            
            <div class="field">
                <div class="label">Message:</div>
                <div class="value"><%= message %></div>
            </div>
            
            <div class="field">
                <div class="label">Submitted at:</div>
                <div class="value"><%= new Date().toLocaleString() %></div>
            </div>
        </div>
        
        <div class="footer">
            <p>This is an automated message from the SendIt contact form.</p>
        </div>
    </div>
</body>
</html> 